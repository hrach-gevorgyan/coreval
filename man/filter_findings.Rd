% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/result_tools.R
\name{filter_findings}
\alias{filter_findings}
\title{Narrow a result to the findings you care about}
\usage{
filter_findings(
  result,
  triage = NULL,
  dataset = NULL,
  rule = NULL,
  variable = NULL
)
}
\arguments{
\item{result}{A result from \code{\link[=check_dataset]{check_dataset()}} or \code{\link[=check_study]{check_study()}}.}

\item{triage}{Keep only these triage levels. One or more of:
\itemize{
\item \code{"wrong value"} - the data contains something that breaks the rule, like
a month of 13. Nothing about your study explains it away. Start here.
\item \code{"missing required"} - something the standard marks Required is absent.
\item \code{"missing optional"} - something Expected is absent, or a value is
blank. Often legitimate: a screen-failure subject with no reference
dates, a variable your raw data does not carry yet.
}

These are coreval's own triage, not a CDISC severity: the rules carry no
severity field, and this does not map onto anyone's scale.}

\item{dataset}{Keep only these datasets, e.g. \code{"AE"}.}

\item{rule}{Keep only these rule ids, e.g. \code{"CORE-000547"}.}

\item{variable}{Keep only findings naming these variables.}
}
\value{
A \code{coreval_result} holding the matching findings. \verb{$skipped} is
left alone: what could not be checked does not become less true because
you narrowed what you are looking at.
}
\description{
Saves writing \code{subset()} over \verb{$findings} by hand, and - because it returns
a result rather than a plain table - what comes back still prints as a
readable report.
}
\examples{
ae <- data.frame(
  STUDYID = "S1", DOMAIN = "AE", USUBJID = c("01", "01"),
  AESEQ = c(1, 2), AETERM = c("Headache", "Rash"),
  AESTDTC = c("2024-01-10", "2024-02-30")
)
result <- check_dataset(ae)

# Just the things that are definitely wrong:
filter_findings(result, triage = "wrong value")
}
