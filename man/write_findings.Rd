% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/export.R
\name{write_findings}
\alias{write_findings}
\title{Write conformance findings to a file}
\usage{
write_findings(result, path, tracking = TRUE)
}
\arguments{
\item{result}{A result from \code{\link[=check_dataset]{check_dataset()}} or \code{\link[=check_study]{check_study()}}.}

\item{path}{Output file path. The extension decides the format: \code{.xlsx}
for Excel, \code{.csv} (or anything else) for CSV.}

\item{tracking}{Add the empty \code{Status}/\code{Owner}/\code{Notes} columns. \code{TRUE} by
default; set \code{FALSE} for a file you intend to read back into R.}
}
\value{
The paths actually written, invisibly. One element for Excel (a
single workbook). For CSV, one path per file written: the findings, plus
siblings for \code{skipped} and \code{about}, plus one for \code{truncated} when any rule
flagged more records than were kept.
}
\description{
Saves the result of \code{\link[=check_study]{check_study()}} to CSV or Excel, so findings can be
shared with people who don't use R, tracked in a spreadsheet, or attached
to a data-review document.
}
\details{
Both tables are always written, never just the findings. A short findings
table can mean clean data, or it can mean many rules were skipped, and
those two situations look identical if the skipped table is dropped:
\itemize{
\item \strong{Excel} (\code{.xlsx}) — one workbook, two sheets: \code{findings} and \code{skipped}.
\item \strong{CSV} (\code{.csv}) — two files, since CSV has no notion of sheets. Findings
go to \code{path}; skipped rules go to a sibling file with \verb{_skipped} before
the extension (\code{issues.csv} gives \code{issues_skipped.csv}).
}

Excel output needs the \code{writexl} package. It is a \code{Suggests}, so if it
isn't installed you get a clear message telling you to install it or use
\code{.csv} instead, rather than a failure part-way through writing.
}
\section{Columns for tracking}{

Three empty columns are added to the findings — \code{Status}, \code{Owner} and
\code{Notes} — for you to fill in by hand once the file is open. They exist so a
finding you have looked at and decided not to act on ("expected, see
protocol deviation log") can be recorded next to the finding itself, rather
than in a separate document nobody reads.
}

\examples{
dir <- tempfile("coreval_study_")
dir.create(dir)
haven::write_xpt(data.frame(USUBJID = c("1", "2"), AGE = c(30, 65)), file.path(dir, "dm.xpt"))
study <- read_study(dir)
result <- check_study(study)

out <- file.path(tempdir(), "findings.csv")
write_findings(result, out)

unlink(dir, recursive = TRUE)
}
