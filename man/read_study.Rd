% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/read.R
\name{read_study}
\alias{read_study}
\title{Read a study into coreval's internal representation}
\usage{
read_study(path)
}
\arguments{
\item{path}{Directory path.}
}
\value{
A study object: \verb{list(datasets = <named list of domain -> list(data, meta, label)>, define = NULL, ct = NULL, standard = list(product, version))}. Each \code{data} is a \code{\link[data.table:data.table]{data.table::data.table()}};
each \code{meta} is a data.table with columns \code{variable}, \code{label}, \code{type};
\code{label} is the dataset's own label (e.g. \code{"Adverse Events"}), or \code{NA} if
unavailable. \code{standard} is the study's declared standard/version (e.g.
\code{list(product = "SDTMIG", version = "3-4")}), read from a CORE test
case's \code{.env} file - both \code{NA} for a real XPT-based study (no \code{.env}).
}
\description{
Detects whether \code{path} is a directory of XPT datasets (a real study) or a
CORE test-case \verb{data/} directory (\verb{_variables.csv} + one CSV per dataset,
usually also \code{.env} and \verb{_datasets.csv}), and reads either into the same
internal representation, so the evaluator never has to know which one it
got.
}
\details{
Character columns use \code{""} for blank/missing (never \code{NA}) to match how
SAS XPT round-trips blanks; numeric columns use \code{NA}. Column types are
taken from the source (XPT's own types, or \verb{_variables.csv}'s declared
\code{Char}/\code{Num}) rather than guessed from the data, so numeric-looking
identifiers (e.g. \code{"007"}) are never silently coerced.
}
\examples{
dir <- tempfile("coreval_study_")
dir.create(dir)
haven::write_xpt(data.frame(USUBJID = c("1", "2"), AGE = c(30, 65)), file.path(dir, "dm.xpt"))
study <- read_study(dir)
study$datasets$DM$data
unlink(dir, recursive = TRUE)
}
