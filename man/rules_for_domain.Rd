% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/scope.R
\name{rules_for_domain}
\alias{rules_for_domain}
\title{Rules that apply to a given SDTM domain}
\usage{
rules_for_domain(domain, use_case = NULL, dataset = NULL)
}
\arguments{
\item{domain}{Domain code, e.g. \code{"AE"}.}

\item{use_case}{Optional use case (e.g. \code{"INDH"}). When supplied, also
filters out rules whose \verb{Scope > Use Case} doesn't include it. Rules
with no Use Case constraint always pass, regardless of this argument.}

\item{dataset}{Optional dataset (an element of a \code{\link[=read_study]{read_study()}} object's
\verb{$datasets}). Only needed to resolve a rule's
\code{include_split_datasets} scope flag, which depends on whether this
dataset is one file of a domain split across several - a property of
the data's \code{DOMAIN} column, not of its name. Without it, such rules
are neither narrowed nor widened.}
}
\value{
A \code{\link[data.table:data.table]{data.table::data.table()}} with the same columns as
\code{\link[=list_rules]{list_rules()}}, filtered to matching rules.
}
\description{
Resolves each rule's \code{Scope > Classes} and \code{Scope > Domains} (handling the
\code{"ALL"}/\code{"NONE"} sentinels and \code{"XX--"} prefix wildcards) against \code{domain}.
}
\details{
Dynamically-named domains not in \code{\link[=sdtm_domain_classes]{sdtm_domain_classes()}} (e.g. \code{SUPPAE},
\code{SUPPDM}) resolve to the \code{RELATIONSHIP} class, since every \code{SUPPxx}
dataset follows the \code{SUPPQUAL} template. Associated Persons domains
(\code{APxx}) currently have no class resolution - as of this writing no
bundled rule pairs an \verb{AP--} domain with a specific (non-\code{"ALL"}) Classes
constraint, so this is a known gap rather than a fix, should that change
upstream.
}
\examples{
ae_rules <- rules_for_domain("AE")
nrow(ae_rules)
}
