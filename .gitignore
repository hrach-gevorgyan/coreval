CLAUDE.md
.claude/
.Rproj.user
.Rhistory
.RData
.Ruserdata
*.Rcheck/
*.tar.gz
data-raw/cdisc-open-rules/
data-raw/upstream/
# Both regenerated by data-raw/dump_ct_codelists.py from the pinned engine
# cache, and 46 and 69 MB of it. The other dump_*.csv files are kept because
# they are a megabyte at most and save cloning a 400 MB repository to rebuild
# an .rds; these two are past that trade.
data-raw/ct_codelists.csv
data-raw/ct_pref_terms.csv
# The pinned JSONata download. data-raw/vendor_jsonata.py says where it comes
# from and refuses anything whose checksum is not the pinned one, and the copy
# that ships is tracked under inst/extdata/js, so keeping a second copy here
# would only be a second thing to keep in step.
data-raw/vendor/
tests/conformance/scoreboard.csv
# docs/ is public except for the maintainer's own files.
docs/RELEASING.md
docs/archive/
docs/reference/
tests/conformance/audit_fixtures.json
tests/conformance/.engine-out/
tests/conformance/audit_fixtures.py
vignettes/*.html
vignettes/*.R
vignettes/*_cache/
vignettes/*_files/
